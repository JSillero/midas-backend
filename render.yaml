databases:
  - name: projectdb
    plan: free
    databaseName: project
    user: project

services:
  - type: web
    plan: free
    name: project
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "python -m gunicorn project.asgi:application -k uvicorn.workers.UvicornWorker"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mysitedb
          property: "postgresql://neondb_owner:nfYtvCT6s2EA@ep-still-glitter-a2xv82wq.eu-central-1.aws.neon.tech/midas_hands?sslmode=require"
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4